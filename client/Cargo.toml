[package]
name = "nix-rust-template-client"
version.workspace = true
edition.workspace = true

[dependencies]
nix-rust-template-shared = { version = "0.0.1", path = "../shared" }
yew = { version = "0.21", features = ["csr"] }
gloo = "0.11.0"
