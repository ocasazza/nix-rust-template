[package]
name = "nix-rust-template-client"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
nix-rust-template-shared = { path = "../shared" }

# more complex frontend dependencies
# yew = { version = "0.21", features = ["csr"] }
# gloo = "0.11.0"
